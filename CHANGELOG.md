# Changelog

All notable changes to `filament-dynamic-filter` will be documented in this file.

## 1.1.0 - 2026-02-15

- cache per column with distinct values only
- add optionsQuery callback for custom option sources
- add configurable cache TTL, max options, and cache scope
- memoize query hashes per request and skip cache writes on exceptions

## 1.0.0 - 202X-XX-XX

- initial release
